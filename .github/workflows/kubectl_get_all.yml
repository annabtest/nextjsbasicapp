name: List All kubectl

on:
  workflow_dispatch:  # Allows manual trigger

jobs:
  get_all:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Log in to Azure
    - name: Log in to Azure
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    # Step 2: Set up kubectl
    - name: Set up kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'latest'

    # Step 3: Get AKS Credentials
    - name: Get AKS Credentials
      run: |
        az aks get-credentials --resource-group ${{ secrets.RESOURCE_GROUP }} --name ${{ secrets.AKS_CLUSTER_NAME }} --overwrite-existing

    # Step 4: Get ALL
    - name: Get ALL
      run: |
        echo "Listing remaining deployments and services..."
        kubectl get all
        kubectl get all -n ${{ vars.ENV_TAG }}
